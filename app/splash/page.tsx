import { SplashScreen } from "@/components/SplashScreen";

// Isolated preview for building/tuning the two-video splash. Tunables live at
// the top of components/SplashScreen.tsx (CROSSFADE_MS, DOOR_WIDTH).
export default function SplashPreviewPage() {
  return <SplashScreen />;
}
